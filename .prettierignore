# Obsolete upstream JavaScript sources. Each one is deleted when its plugin is
# ported to plugins/<id>/src/client.ts, so they are never worth reformatting.
plugins/*/src/*.js

# Generated plugin artifacts are committed for direct GitHub installs, but the
# builder owns their formatting. Third-party vendor bundles must never be reformatted:
plugins/*/index.mjs
plugins/*/client/entry.mjs
plugins/*/client/vendor/

# Upstream provider package keeps its own formatting and test harness.
packages/pi-claude-agent-sdk/
