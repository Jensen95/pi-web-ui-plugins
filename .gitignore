# Dependencies
node_modules/

# Build artifacts
dist/

# Release and OS files
release/
.DS_Store

# pi agent data
.pi/
.pi-web/

# Per-plugin runtime dependencies (installed on demand by the host)
plugins/*/node_modules/

# Per-plugin runtime data (user data, never committed)
plugins/*/storage/

# ---------------------------------------------------------------------------
# Generated plugin artifacts.
#
# Compiled entries and vendored browser assets are tracked because the normal
# GitHub subdirectory installer needs a runnable plugin without a local build.
# ---------------------------------------------------------------------------
