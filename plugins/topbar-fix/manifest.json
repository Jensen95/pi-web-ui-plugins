{
	"id": "topbar-fix",
	"name": "Top Bar Fix",
	"version": "0.1.0",
	"icon": "🩹",
	"description": "Stopgap stylesheet for two top-bar overflow menu bugs: pi-web-ui issue 162 clips the menu inside .view-switch, and on 0.87.x the portalled menu clips and flattens the Theme, Language and Sound controls rendered inside it. Delete this plugin once both ship upstream.",
	"build": {
		"install": "npm install --ignore-scripts --no-audit --no-fund esbuild@0.28.2",
		"command": "npx --no-install esbuild src/client.ts --bundle --platform=browser --format=esm --target=es2022 --loader:.css=text --external:./vendor/* --outfile=client/entry.mjs",
		"outputs": ["client/entry.mjs"]
	}
}
